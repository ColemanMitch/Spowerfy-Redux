{
  "name": "oauth-bridge-template",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "ts-node src/server.ts",
    "pull-creds": "cypress run --spec cypress/integration/retrieve-api-keys.spec.ts --browser chrome"
  },
  "dependencies": {
    "express": "^4.15.4",
    "querystring": "^0.2.0",
    "request": "^2.81.0"
  },
  "devDependencies": {
    "@types/jasmine": "^4.0.3",
    "@types/jest": "^27.4.1"
  }
}
